# parking-manager — Quick Start

This repository contains a Node.js backend (`backend/`) and a Flutter frontend (`frontend/`).

Prerequisites:
- Node.js & npm (for the backend) 
- Flutter setup (for the frontend)
- MySQL server(Xampp)

Quick Start (PowerShell)

1) Prepare the database (MySQL)
   -Open Xampp and start the apache and MySQL
2) Start backend (new terminal)

```powershell
cd backend
npm install
npm run start
```

3) Start frontend (separate terminal)

```powershell
cd frontend
flutter pub get
flutter run     
```

## Table of contents
- Prerequisites
- Install Node.js (Windows)
- Install Flutter (Windows)
- Install Xampp (or run with Docker)
- Project setup — backend
- Project setup — frontend
- Running backend + frontend together
